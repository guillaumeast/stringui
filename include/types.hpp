#pragma once

#include <string>
#include <vector>
#include <optional>

// Let's make C++ syntax more smooth ✌️
using string = std::string;
// Let's make C++ syntax more smooth ✌️
template<typename T> using vector = std::vector<T>;
// Let's make C++ syntax more smooth ✌️
template<typename T> using opt = std::optional<T>;

